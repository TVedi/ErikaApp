/**
 * DRAFT — placeholder legal content. Real legal copy must be supplied/reviewed by a human before taking live payments.
 */
export function DraftLegalBanner() {
  return (
    <div
      className="mb-8 rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900"
      role="status"
    >
      <strong>DRAFT</strong> — placeholder, not legal advice; replace before production.
    </div>
  );
}
